Semantic-Django is a project to create an OSM (object semantic mapper).

Installation requirements are on file requirements.txt and can be installed using: pip install -r requirements.txt

Currently being developed a Django backend compatible with Virtuoso.

Next steps: 
  - Set of update SPARQL query when the object is saved
  - Create semantic django compatibility with django forms and django admin